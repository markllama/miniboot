#!ipxe
kernel http://{{ network.addr }}:{{ http.port }}/coreos/kernel initrd=main coreos.live.rootfs_url=http://{{ network.addr }}:{{ http.port }}/coreos/rootfs.img ignition.platform.id=metal ignition.firstboot ignition.config.url=http://{{ network.addr }}:{{ http.port }}/{{ clients[client_number | int].name }}-config.ign console={{ console }}
initrd --name main http://{{ network.addr }}:{{ http.port }}/coreos/initrd.img
boot
