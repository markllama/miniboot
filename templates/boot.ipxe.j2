#!ipxe
kernel http://{{ network.addr }}/coreos/kernel initrd=main coreos.live.rootfs_url=http://{{ network.addr }}/coreos/rootfs.img ignition.platform.id=metal ignition.firstboot ignition.config.url=http://{{ network.addr }}/{{ clients[client_number | int].name }}-config.ign console={{ console }}
initrd --name main http://{{ network.addr }}/coreos/initrd.img
boot
