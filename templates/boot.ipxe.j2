#!ipxe
kernel http://{{ server.addr }}/coreos/kernel initrd=main coreos.live.rootfs_url=http://{{ server.addr }}/coreos/rootfs ignition.platform.id=metal ignition.firstboot ignition.config.url=http://{{ server.addr }}/config.ign console={{ client.console }}
initrd --name main http://{{ server.addr }}/coreos/initrd
boot